# Live Run Step 3 Docs Package

Purpose: convert the live registry outputs into a normalized Google Docs/Drive target manifest for one client.

This package does not fetch Google Docs content directly. It prepares the exact target set that the runtime should resolve through a Google Docs/Drive connector inside Antigravity.
