Fast path:
1. Copy the `antigravity_stage6` folder beside the earlier stage folders.
2. Run `pip install -r .\\antigravity_stage6\\requirements.txt`
3. Run `python .\\antigravity_stage6\\tests\\test_stage6_smoke.py`
